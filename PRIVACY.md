# 数据访问与隐私说明

本文描述当前源码的实际行为，不是“零数据访问”承诺。Codex Writer 是运行在用户机器上的
控制工具，没有项目自己的数据收集服务器；但它必须读取任务和进程信息才能判断占用者。
“本地处理”也不等于这些信息不敏感。

## 1. 会读取什么

| 数据来源 | 读取内容 | 目的和范围 |
| --- | --- | --- |
| `~/.config/session-writer/config.json`，或 `SESSION_WRITER_CONFIG` 指定的文件 | 配置的 Codex 目录、程序路径、SSH 别名、Mac 后台/Python 路径、交接开关、排除的任务 ID | 定位用户明确指定的资源；不自动登记新机器 |
| 用户的 `~/.ssh/config` | 读取配置文件文本；提取具名 Host 的别名、HostName、User、Port | 列出候选 SSH 主机和关联连接来源；不是网络扫描 |
| Mac 的 `CODEX_HOME/state_*.sqlite` | 当前选中的最新编号索引中的任务 ID、名称、标题、preview 摘要、创建/更新时间、来源、代理路径、归档/置顶状态 | 通过 SQLite 只读连接查询。目前读取整个 threads 表的这些字段，而不只是界面显示的最近 60 条 |
| Mac 的写锁目录与文件句柄 | 锁文件名、对应任务 ID、inode、打开文件的 PID | 判断谁实际持有任务写入资源；不靠删除锁解除占用 |
| Mac 进程表、Unix socket、已建立的 TCP 连接 | PID、父 PID、系统用户 ID、启动时间、程序命令行、连接地址 | 关联 Codex 后台、SSH 代理和真实对端。进程枚举不只看到当前任务的进程 |
| Windows 的系统主机名及进程列表 | 可访问的 Win32_Process 记录，包括名称、路径、命令行、父进程、创建时间 | 筛选 Codex 桌面客户端和 SSH 代理；回传筛选后的身份摘要，不回传完整进程表 |
| Codex 本地 app-server / 桌面通信 | 已加载任务、运行状态、回合 ID、最近回合元数据；某些桌面状态快照可能包含对话历史 | 用于影响范围、状态和中断后的恢复判断 |
| 本工具自己的逐主机缓存、日志和回执 | 绑定 SSH 配置的身份检测、交接结果、恢复名单、发送状态及诊断元数据 | 防止不同主机相同 PID 被混淆，拒绝状态不明时的重复操作 |

### 对话内容的准确边界

不要把本项目描述成“绝不读取对话内容”：

- 任务索引的标题和 `preview` 字段本身可能包含用户输入或任务内容。
- 桌面接口可能推送含历史的状态快照。当前实现从中提取状态和 ID；较大的响应经本地
  `jq` 流式筛选，较小的响应会临时解码在进程内存里。
- 正常事件日志不以保存聊天正文为目的，也不主动把收到的完整历史写成导出文件。
  这不保证操作系统交换区、崩溃转储或第三方程序从未保存相关内存。
- 恢复检查会启动短生命周期的本地 Codex app-server，只调用限定的读取方法；
  Codex 自己如何访问登录状态、写日志或联网，由其配置和实现决定。

## 2. 什么不会被本工具主动读取或收集

- 没有读取 OpenAI 登录令牌用于机器发现的代码路径，不读取账号设备目录或账号发现缓存。
- 不调用 OpenAI 同账号设备发现、配对和中转接口，也没有相关启用开关。
- 不读取 Tailscale 账号设备 API，不扫描局域网，不发现未经用户登记的机器。
- 不为诊断主动抓屏、录音、读取剪贴板，或复制整个代码仓库和整个 Codex 数据目录。
- 不向维护者上传日志、聊天记录、使用量、崩溃报告或遥测。

以上描述针对 **Codex Writer 自身**。OpenSSH 会使用用户配置的私钥、SSH agent、known_hosts
等认证材料；本工具不复制这些材料进仓库或日志。真实 SSH 连接仍遵循用户的 OpenSSH
配置，可能运行其中的连接辅助命令；被动主机列表读取则不执行 Include/Match/ProxyCommand。

## 3. 数据保存在哪里

| 位置 | 内容与保留方式 |
| --- | --- |
| 数据主机：`~/.local/state/session-writer/`，或 `SESSION_WRITER_STATE_DIR` | `controller-<配置摘要>-cache.json` 逐主机身份缓存、最近交接结果、当前操作锁；文件按需更新，没有按天自动清理 |
| Mac：上述目录的 `logs/writer.jsonl` 及轮转文件 | 有界结构化事件日志；单文件目标上限约 2 MiB，最多 5 份（含当前文件） |
| 数据主机：`logs/force-handoff.json` | 强制交接阶段、受影响任务/代理、回合与进程 ID、已关闭控制端及保留后台、继续请求和送达状态；不是完整聊天历史 |
| Windows：`%LOCALAPPDATA%\SessionWriter\logs\` | 本机 UI 日志 `windows-ui.jsonl` 及最多 2 个旧文件，约 2 MiB 时轮转；单次追加可能使其略超阈值 |
| Windows：同目录的 `backend-last.json` | 用户请求查看/导出日志时保存的最近至多 100 条 Mac 事件；总导出限制为 2 MiB |
| 显式指定的 UI 自测输出路径 | 仅在用户运行带截图输出参数的自测模式时产生，不是正常后台收集 |
| 数据主机的 `codex-writer-request-*` 临时目录 | Windows 默认模式下，本次程序源码、允许的工具配置；正常退出删除，不安装服务。强行终止可能遗留含路径/主机别名的临时目录 |

安装文件、配置、状态相互分离。仓库 `.gitignore` 和发布检查排除真实配置、日志、缓存、
任务数据库、SSH 私钥和构建产物。本工具使用独立日志目录。

事件日志采用字段白名单和敏感模式过滤；缓存、回执、错误说明及导出文件仍可能含主机名、
IP、任务名称、任务 ID、代理路径或操作时间。**过滤不是全面匿名化保证。** 分享前请人工检查，
不要把认证文件、私钥或整个 `CODEX_HOME` 附到 issue。

## 4. 什么会在机器间传输

用户配置的 SSH 两端之间会传输：

- Windows/macOS 控制端的主机名称和筛选后的进程摘要。
- Mac 的任务列表、占用情况、影响范围、诊断和交接结果。
- 明确选定的操作、任务 ID、目标 ID、状态校验值和需要关闭的精确进程身份。
- 用户要求查看的日志片段。
- Windows 独立运行模式下的本工具应用源码、操作参数及允许的工具设置。程序采用固定
  文件名白名单；不打包密码字段、SSH 私钥、登录文件或完整 SSH 配置。临时后端仍使用
  数据主机上用户已有的 SSH 配置和认证。

没有项目方服务器或 OpenAI 账号中转作为备用通道。VPN/Tailscale 自身的控制平面、网络中继、
日志和隐私条款由该网络产品决定；“不走 OpenAI 中转”不等于保证底层没有任何网络中继。

若选择接管后继续，工具会向 Codex 提交正常的继续请求，其中可能包含任务/代理/回合 ID。
Codex 随后可能将上下文发送到其配置的模型服务，产生网络流量和额度消耗；这是用户请求的
任务恢复，不是 Codex Writer 的遥测。软件不改变 Codex、模型提供方或组织的隐私设置。

## 5. 只读与有副作用操作

自动刷新和排错不接管任务、不修改任务历史，但会读取上述数据、进行受限的 SSH 身份查询，
并更新本工具的缓存与日志。因此“对任务只读”不等于“不写任何本地文件”。

用户启用并确认交接后，软件可能关闭原桌面客户端或后台进程，启动目标客户端、恢复任务，
并按选择发送继续。这会间接让 Codex 更新任务状态或执行原任务中的文件操作。

## 6. 停用和清理

关闭本工具并保持 `enable_handoff: false` 即可停止通过本工具发起新交接。没有本项目安装的
常驻后台服务。远端辅助代码仅在请求期间运行；停用不自动关闭已经运行的 Codex 任务或用户自行部署的 SSH/VPN 服务。

删除缓存或回执前先核对未完成操作；删掉不确定送达回执可能失去防重复依据。需要卸载时，
只处理本工具独立的安装、配置和状态目录，保留必要备份；不要删除 `.codex`、SSH 配置或其他软件。

技术对应见 [architecture](docs/architecture.md)、`app/discovery_adapter.py`、
`app/writer_service.py`、`app/writer_desktop.py`、`app/writer_log.py` 和 `app/unlock.ps1`。
